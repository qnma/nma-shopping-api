{
  "type": "mysql",
  "host": "localhost",
  "port": 9006,
  "username": "lin",
  "password": "lin",
  "database": "nma_shopping",
  "synchronize": true,
  "logging": true,
  "entities": ["src/**/*.entity.ts"],
  "migrations": ["_migration/*.js"],
  "cli": {
    "migrationsDir": "_migration"
  }
}
